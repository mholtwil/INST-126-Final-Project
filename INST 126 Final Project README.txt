Introduction:
	This program will generates passwords that do not repeat. The passwords will also follow all the users guidelines such as the necessary length or character requirements. Essentially bot will generate unique passwords that satisfy the users needs.

Requirements:
	The requirements are any generic coding environment.
